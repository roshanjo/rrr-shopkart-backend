BEGIN;
--
-- Create model Product
--
CREATE TABLE "api_product" ("id" integer NOT NULL PRIMARY KEY, "title" varchar(255) NOT NULL, "price_inr" integer NOT NULL, "category" varchar(100) NOT NULL);
--
-- Create model Address
--
CREATE TABLE "api_address" ("id" bigint NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, "full_name" varchar(100) NOT NULL, "phone" varchar(15) NOT NULL, "street" text NOT NULL, "city" varchar(50) NOT NULL, "state" varchar(50) NOT NULL, "pincode" varchar(10) NOT NULL, "created_at" timestamp with time zone NOT NULL, "user_id" integer NOT NULL UNIQUE);
ALTER TABLE "api_address" ADD CONSTRAINT "api_address_user_id_19458536_fk_auth_user_id" FOREIGN KEY ("user_id") REFERENCES "auth_user" ("id") DEFERRABLE INITIALLY DEFERRED;
COMMIT;
